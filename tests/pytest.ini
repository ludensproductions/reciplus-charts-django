[pytest]
addopts = --html=reports/e2e_report.html --self-contained-html
markers =
    slow: mark tests as slow
    smoke: mark smoke tests
    edge: mark edge tests

python_files = tests.py, test_*.py
testpaths = tests
