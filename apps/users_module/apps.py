from django.apps import AppConfig


class UsersModuleConfig(AppConfig):
    """Configuration for the users module application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.users_module"
