from django.apps import AppConfig


class VehicleBrandsConfig(AppConfig):
    """Configuration for the vehicle brands app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.vehicle_brands"
