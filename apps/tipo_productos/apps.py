from django.apps import AppConfig


class TipoProductosConfig(AppConfig):
    """Configuration for the TipoProductos app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.tipo_productos"
