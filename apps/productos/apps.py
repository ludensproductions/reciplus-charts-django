from django.apps import AppConfig


class ProductosConfig(AppConfig):
    """Configuration for the Productos app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.productos"
