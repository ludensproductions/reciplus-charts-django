from django.apps import AppConfig


class Select2Config(AppConfig):
    """Configuration for the select2 app, which provides enhanced select widgets for Django forms."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.select2"
